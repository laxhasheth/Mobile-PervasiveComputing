<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/alert_light_frame"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieSearchApp"
        tools:targetApi="31">

        <!-- NEW: Login is now launcher -->
        <activity
            android:name=".ui.view.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MovieSearchApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.view.RegisterActivity" />
        <activity android:name=".ui.view.MainActivity" />
        <activity android:name=".ui.view.MovieDetailsActivity" />
        <activity android:name=".ui.view.MovieListActivity" />
        <activity android:name=".ui.view.AddEditMovieActivity" />

    </application>

</manifest>
